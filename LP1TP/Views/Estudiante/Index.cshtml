@model IEnumerable<LP1TP.Models.Estudiante>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("CREAR NUEVO", "Create")
</p>
<table class="table table-striped table-bordered" id="TablaIndex">
    <tr>
        <th></th>
        <th>
            @Html.DisplayName("CODIGO ESTUDIANTE")
        </th>
        <th>
            @Html.DisplayName("ESTUDIANTE")
        </th>
        <th>
            @Html.DisplayName("EDAD")
        </th>
        <th>
            @Html.DisplayName("CORREO")
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumeroEstudiante)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.Persona.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Persona.Edad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Persona.Correo)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.Estudiante_Id }) |
                @Html.ActionLink("Detalle", "Details", new { id = item.Estudiante_Id }) |
                @Html.ActionLink("Borrar", "Delete", new { id = item.Estudiante_Id })
            </td>
        </tr>
    }

</table>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")

<script>
  
</script>
 }
